export default async function Dashboard() {
	try {
	} catch (error) {
		console.log(error)
		return <div>Error</div>
	}
	return (
		<div>
			<h1>Dashboard</h1>
			tasks will be here for employee
			if empolyee render employee dashboard
			if admin render admin dashboard 
		</div>
	)
}
